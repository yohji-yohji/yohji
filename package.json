{
  "name": "yoji-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site with a Node.js backend server for Linux deployment.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
